<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="@dimen/padding_size"
    android:orientation="vertical">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/gap1">
                <ImageView style="@style/PanelHeader"
                    android:id="@+id/imageBackAction"
                    android:src="@drawable/arrow_back"
                    android:foreground="?attr/selectableItemBackground"/>
                <TextView style="@style/PanelHeader"
                    android:text="@string/settings"
                    android:paddingStart="@dimen/padding_size"/>
        </LinearLayout>

        <androidx.appcompat.widget.SwitchCompat style="@style/SettingsActivityText"
            android:id="@+id/themeSwitcher"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/padding_size"
            android:text="@string/dark_theme"
            android:paddingBottom="@dimen/gap1"/>

        <FrameLayout
            android:id="@+id/layoutShare"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/gap1"
            android:foreground="?attr/selectableItemBackground">
                <TextView style="@style/SettingsActivityText"
                    android:text="@string/share_app" />
                <ImageView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:src="@drawable/share"
                    android:layout_gravity="end"/>
        </FrameLayout>

        <FrameLayout
            android:id="@+id/layoutSupport"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/gap1"
            android:foreground="?attr/selectableItemBackground">
                <TextView style="@style/SettingsActivityText"
                    android:text="@string/support" />
                <ImageView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:src="@drawable/support"
                    android:layout_gravity="end" />
        </FrameLayout>

        <FrameLayout
            android:id="@+id/layoutUserAgreement"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/gap1"
            android:foreground="?attr/selectableItemBackground">
                <TextView style="@style/SettingsActivityText"
                    android:text="@string/user_agreement" />
                <ImageView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:src="@drawable/arrow_forward"
                    android:layout_gravity="end"/>
        </FrameLayout>

</LinearLayout>